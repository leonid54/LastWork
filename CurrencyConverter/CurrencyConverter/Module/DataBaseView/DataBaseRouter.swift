import UIKit

final class DataBaseRouter {
    private var controller: DataBaseViewController?

    func setRootController(controller: DataBaseViewController) {
        self.controller = controller
    }
}
